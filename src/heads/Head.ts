/**
 * The head is a superstructure that configures a coffee-front instance.
 * The DevHead and LiveHead contain separate configurations. Each head
 * is an instance of coffee-front and can communicate to dispatch.
 */
export class Head{
	constructor( config: any ){

	}
}
